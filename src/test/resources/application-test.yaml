
#h2 is only used here to allow the tests to execute
spring:
  datasource:
    url: jdbc:h2:mem:test_mem
    driverClassName: org.h2.Driver
    #username: sa
    #password: null

logging:
  pattern:
    console: "%date{yyyy-MM-dd/HH:mm:ss.SSS/zzz} %-5p \\(%file:%line\\) [%thread] :: %msg %n"
  level:
    root: INFO
    com:
      github:
        xcrigx: TRACE
